name: psc-action-new
on:
  push:
    branches: [ main, master, develop, stage, release ]
  pull_request:
    branches: [ main, master, develop, stage, release ]
  workflow_dispatch:  
jobs:
  build:
    runs-on: [ ubuntu-latest ]
    steps:
      - name: PSC Upload files
        run: | 
          curl --header 'authorization: Bearer AAAQrYi0FCEyvxFzMnuX04rTwnkJpPFvXBHuXCSLOv2YKeqH' --header 'Group: 18725' -T 7z1900-x64.exe 'https://protecode-sc.com/api/upload/'
